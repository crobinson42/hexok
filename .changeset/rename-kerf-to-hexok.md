---
"hexok": major
---

Rename the library from Kerf to Hexok (Hexo Kit). Install with `npm i hexok` and import layers as `hexok/core`, `hexok/domain`, `hexok/app`, `hexok/infra`, `hexok/runtime`, and `hexok/testing`. Completeness and runtime diagnostics use the `hexok:` prefix.
